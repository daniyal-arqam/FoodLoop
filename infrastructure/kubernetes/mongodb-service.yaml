apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: foodloop
  labels:
    app: mongodb
    app.kubernetes.io/part-of: foodloop
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - name: mongo
      port: 27017
      targetPort: mongo
