# FoodLoop Frontend

React (Vite) client for FoodLoop.

This scaffold shows a health status page only. Auth, listings, and AI UI are added later.

## Run

```bash
npm install
npm run dev
```

- App: `http://localhost:5173`
- Health: `http://localhost:5173/health.json`
